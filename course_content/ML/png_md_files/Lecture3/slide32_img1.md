The image contains a snippet of Python code using the `sklearn.cluster` library. 

1. **First Line:**
   - Text: `from sklearn.cluster import MiniBatchKMeans`
   - The word "from" is in light blue.
   - The module name "sklearn.cluster" is in deep blue.
   - The word "import" is in light blue.
   - The class name "MiniBatchKMeans" is in deep blue.
   
2. **Second Line:**
   - Text: `minibatch_kmeans = MiniBatchKMeans(n_clusters=5)`
   - The variable name "minibatch_kmeans" is in deep blue.
   - The equals sign "=" is black.
   - The class name "MiniBatchKMeans" is in deep blue.
   - Inside the parentheses, "n_clusters" is in black.
   - The equal sign "=" inside the parentheses is in black.
   - The number "5" is in orange.

3. **Third Line:**
   - Text: `minibatch_kmeans.fit(X)`
   - The variable name "minibatch_kmeans" is in deep blue.
   - The method name ".fit" is in deep blue, following the variable without a space.
   - In the parentheses, the letter "X" is in black.

Each line of code is separated by a single line space. The code is set against a plain white background with no additional symbols or annotations outside of the code itself.