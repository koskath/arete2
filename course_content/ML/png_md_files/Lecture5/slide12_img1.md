The image contains two lines of Python code with syntax highlighting.

1. **First Line:**
   - The text is: `from sklearn.decomposition import PCA`.
   - The word `from` is in blue.
   - The text `sklearn.decomposition` is in cyan.
   - The word `import` is in blue.
   - The text `PCA` is in dark blue.

2. **Second Line:**
   - The text is: `pca = PCA(n_components = 2)`.
   - The variable `pca` is in dark blue.
   - The equals sign `=` is in black.
   - The text `PCA` is in dark blue.
   - The text `n_components` is in black.
   - The equals sign `=` is in black.
   - The number `2` is in orange.
   - Parentheses `(` and `)` around `n_components = 2` are in black.