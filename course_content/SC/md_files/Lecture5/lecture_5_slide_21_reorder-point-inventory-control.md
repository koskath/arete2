# Reorder Point Inventory Control

In a reorder point inventory control system, inventory levels are continuously monitored, and orders are issued when the inventory is depleted to a predetermined level, called the reorder point (ROP): ROP = D x L + SS
